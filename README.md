# PROJECT-II

Simple FastAPI backend for SDN management.

## Structure

- `backend/app/main.py`: API entrypoint

## Notes

- Configure OpenDaylight connection values in `backend/.env`
- Do not commit secrets or local virtual environments
